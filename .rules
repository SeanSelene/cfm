# 项目简介

cfm (Config File Manager) 是一个跨平台配置文件管理工具，通过 Git 仓库统一管理各类软件的配置文件。

## 技术方案

- **语言**: Rust (edition 2024)
- **CLI 框架**: clap (derive 宏)
- **配置格式**: TOML (serde 序列化)
- **支持平台**: Windows、Linux、macOS

### 链接模式

- `soft` - 软链接（符号链接）
- `hard` - 硬链接
- `cp` - 复制文件

### 配置文件

- 用户配置: `{config_dir}/cfm/config.toml`
- 仓库配置: 仓库根目录的 `cfm.toml`

---

# 代码质量

每次修改后，执行以下命令：

1. `cargo clippy` - 检查并修复所有错误，尽量修复警告
2. `cargo fmt` - 格式化改动的代码
